# Loader metadata
modLoader = "javafml"
loaderVersion = "[4,)"
license = "MIT"
issueTrackerURL = "https://github.com/eerussianguy/firmalife/issues"

# TFC
[[mods]]
    modId = "${modId}"
    version = "${modVersion}"
    displayName = "Firmalife"
    credits = "EERussianguy, Indi, Peffern, Lumintorious"
    authors = "EERussianguy, Indi, Peffern, Lumintorious"
    description = '''A cooking and farming addon for TFC.'''

# Dependencies
[[dependencies.firmalife]]
    modId = "patchouli"
    type = "required"
    versionRange = "${patchouliVersionRange}"
    ordering = "NONE"
    side = "BOTH"

[[dependencies.firmalife]]
    modId = "jei"
    type = "optional"
    versionRange = "${jeiVersionRange}"
    ordering = "NONE"
    side = "CLIENT"

[[dependencies.firmalife]]
    modId = "neoforge"
    type = "required"
    versionRange = "${neoForgeVersionRange}"
    ordering = "AFTER"
    side = "BOTH"

[[dependencies.firmalife]]
    modId = "minecraft"
    type = "required"
    versionRange = "${minecraftVersionRange}"
    ordering = "AFTER"
    side = "BOTH"

[[mixins]]
    config = "${modId}.mixins.json"

# We do our own biome coloring, see #2279
[mods."sodium:options"]
    "mixin.features.fast_biome_colors"=false  # Rubidium
    "mixin.features.render.world.sky"=false  # Embeddium, probably Sodium as well
